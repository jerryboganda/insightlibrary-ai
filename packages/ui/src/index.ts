// Shared Svelte 5 component library. shadcn-svelte + LayerChart land here in
// Phase 0/2 as screens get built; components must stay framework-token driven
// (Tailwind semantic variables from apps/app/src/app.css).
export { default as Button } from './components/Button.svelte';

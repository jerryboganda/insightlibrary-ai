# parser-svc dependencies (Phase 4). Docling (MIT) is the primary parser.
# MinerU (AGPL), if ever added for hard pages, would be isolated in THIS
# container ONLY and never linked into insight-core.
#
# Installed with `uv pip install --torch-backend=cpu` (see Dockerfile), which:
#   * resolves docling's large transitive tree in seconds — pip's resolver
#     backtracks exponentially through docling-core / docling-ibm-models
#     versions and dies with ResolutionTooDeep on this tree;
#   * routes torch/torchvision to the CPU-only wheels automatically, so the
#     multi-GB CUDA wheels are never fetched (critical on the ~6GB shared VPS).
# torch/torchvision are intentionally unpinned here — they come in as docling
# deps and uv's --torch-backend=cpu selects the CPU builds.
fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.9
pillow>=10.4

# Docling: structure-aware PDF/office parser (layout, tables, reading order).
# Use a recent line, NOT the old 2.15.1 pin: 2.15.1 imports names from
# docling-core that were removed in current docling-core, and its loose
# sub-dep pins let uv resolve an incompatible pair (ImportError: BoundingBox).
# A recent docling ships coherent, mutually-tested docling-core/-ibm-models.
docling>=2.40,<3
